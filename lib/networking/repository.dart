import 'package:sudoku/calculations/Matrix.dart';
import 'package:sudoku/data/items.dart';

const String theNewsDBApiKey = 'b55dbf06c8214c6884da6c9ef7a56e22';

class Repo{
//  Items items = Items();
  Matrix matrix = Matrix(1);

//  Items fetchItems() => items;
  Matrix fetchMatrix() => Matrix(1);

 // void  resetItems(){items = Items();}
 // void pushItems(){

//  }
//  Future<NewsListResponse> fetchNews(String country,int pageSize, int pageNumber) async =>
//      await _restClient.getTasks(country,pageSize,pageNumber,theNewsDBApiKey );
}